import { createAction } from '@ngrx/store';

export const toggleThemeMode = createAction('[Theme] Toggle Theme Mode');
export const toggleThemeTemperature = createAction('[Temperature] Toggle Theme Temperature');
